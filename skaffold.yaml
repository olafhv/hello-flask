apiVersion: skaffold/v3
kind: Config
metadata:
  name: hello-flask

deploy:
  kubectl: {}

manifests:
  rawYaml:
     - deployment.yaml

build:
  artifacts:
  - image: hello-flask

  # Comment following lines below to build locally
  cluster:
    dockerConfig:
      secretName: kaniko
